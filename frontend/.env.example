# Frontend (copy to frontend/.env.local for local work)
# Leave NEXT_PUBLIC_API_URL empty in local dev so Next.js proxies /api to the backend.
# On Vercel set this to your Render API URL, with no trailing slash.
NEXT_PUBLIC_API_URL=
BACKEND_URL=http://localhost:4000
